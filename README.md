# SCG Timer

GitHub Pages wrapper for [SCG Timer](https://stockholmcode.github.io/scg-timer).

This repo only hosts the iframe that points to the Apps Script deployment. The actual source code is in a separate private repository.
